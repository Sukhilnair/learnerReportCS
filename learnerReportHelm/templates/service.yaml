apiVersion: v1
kind: Service
metadata:
  name: {{ include "learnerReport.fullname" . }}
  labels:
    {{- include "learnerReport.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learnerReport.selectorLabels" . | nindent 4 }}
